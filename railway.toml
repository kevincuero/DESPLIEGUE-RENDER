[build]
builder = "nixpacks"
builderOptions = { nixpacks = { provider = "python" } }

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT main:app"
healthcheckPath = "/"
restartPolicyType = "never"

[variables]
FLASK_ENV = "production"
PYTHON_VERSION = "3.11"